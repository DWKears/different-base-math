#Making a digital list to convert to maximum Fluette elements
maxbase = 16
dig = list(range(maxbase))
dig.pop(0)

maxF = []

for i in range(maxbase-2):
    bmf = baseToInt10(dig[:i+1],i+2)
    maxF.append(bmf)

print(maxF)
